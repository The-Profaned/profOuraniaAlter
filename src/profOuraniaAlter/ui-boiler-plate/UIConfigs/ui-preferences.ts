export const loadUiPreferencesIntoState = (): void => {
	// Intentionally disabled: UI selections are now session-only.
};

export const persistUiPreferencesFromState = (): void => {
	// Intentionally disabled: UI selections are now session-only.
};
